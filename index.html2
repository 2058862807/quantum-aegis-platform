<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quantum Aegis - Enterprise Cyber Defense Platform</title>
    <style>
        :root {
            --quantum-blue: #00d4ff;
            --quantum-purple: #8b5cf6;
            --quantum-cyan: #06b6d4;
            --dark-900: #0c0a1a;
            --dark-800: #1a1625;
            --dark-700: #2d2438;
            --threat-critical: #ef4444;
            --threat-warning: #f59e0b;
            --threat-safe: #10b981;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background: var(--dark-900);
            color: #e5e7eb;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        
        /* Navigation */
        .nav {
            background: rgba(26, 22, 37, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 212, 255, 0.2);
            padding: 1rem 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 100;
        }
        
        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--quantum-blue), var(--quantum-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }
        
        .nav-links a {
            color: #e5e7eb;
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .nav-links a:hover {
            color: var(--quantum-blue);
        }
        
        .cta-button {
            background: linear-gradient(135deg, var(--quantum-blue), var(--quantum-purple));
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.3s;
        }
        
        .cta-button:hover {
            transform: translateY(-2px);
        }
        
        /* Hero Section */
        .hero {
            padding: 8rem 0 4rem;
            text-align: center;
            background: radial-gradient(circle at center, rgba(0, 212, 255, 0.1) 0%, transparent 70%);
        }
        
        .hero h1 {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--quantum-blue), var(--quantum-purple), var(--quantum-cyan));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .hero p {
            font-size: 1.2rem;
            color: #9ca3af;
            margin-bottom: 2rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }
        
        /* Dashboard */
        .dashboard {
            padding: 4rem 0;
        }
        
        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }
        
        .metric-card {
            background: rgba(26, 22, 37, 0.8);
            border: 1px solid rgba(0, 212, 255, 0.2);
            border-radius: 12px;
            padding: 1.5rem;
            transition: transform 0.3s, border-color 0.3s;
        }
        
        .metric-card:hover {
            transform: translateY(-4px);
            border-color: var(--quantum-blue);
        }
        
        .metric-label {
            color: #9ca3af;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
        
        .metric-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--quantum-blue);
        }
        
        .metric-change {
            font-size: 0.8rem;
            color: var(--threat-safe);
        }
        
        /* Threats Section */
        .threats-section {
            background: rgba(26, 22, 37, 0.8);
            border-radius: 12px;
            padding: 2rem;
            margin-bottom: 3rem;
        }
        
        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            color: var(--quantum-blue);
        }
        
        .threat-item {
            background: rgba(45, 36, 56, 0.6);
            border-left: 4px solid var(--threat-critical);
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 6px;
        }
        
        .threat-title {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .threat-meta {
            display: flex;
            gap: 1rem;
            font-size: 0.8rem;
            color: #9ca3af;
        }
        
        .status-badge {
            background: var(--threat-critical);
            color: white;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            font-size: 0.7rem;
            font-weight: 600;
        }
        
        /* Pricing */
        .pricing {
            padding: 4rem 0;
            text-align: center;
        }
        
        .pricing h2 {
            font-size: 2.5rem;
            margin-bottom: 3rem;
            background: linear-gradient(135deg, var(--quantum-blue), var(--quantum-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .pricing-card {
            background: rgba(26, 22, 37, 0.8);
            border: 1px solid rgba(0, 212, 255, 0.2);
            border-radius: 12px;
            padding: 2rem;
            transition: transform 0.3s, border-color 0.3s;
        }
        
        .pricing-card:hover {
            transform: translateY(-8px);
            border-color: var(--quantum-blue);
        }
        
        .plan-name {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--quantum-blue);
        }
        
        .plan-price {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }
        
        .plan-features {
            list-style: none;
            margin-bottom: 2rem;
        }
        
        .plan-features li {
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(0, 212, 255, 0.1);
        }
        
        .select-plan {
            background: linear-gradient(135deg, var(--quantum-blue), var(--quantum-purple));
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            transition: transform 0.3s;
        }
        
        .select-plan:hover {
            transform: translateY(-2px);
        }
        
        /* Animations */
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        .pulse { animation: pulse 2s infinite; }
        
        /* Global Threat Map */
        .map-section {
            background: rgba(26, 22, 37, 0.8);
            border-radius: 12px;
            padding: 2rem;
            margin-bottom: 3rem;
        }
        
        .map-container {
            position: relative;
            background: rgba(12, 10, 26, 0.8);
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 2rem;
            border: 1px solid rgba(0, 212, 255, 0.3);
        }
        
        #world-map {
            position: relative;
            width: 100%;
            height: 400px;
            overflow: hidden;
        }
        
        .world-svg {
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, rgba(0, 212, 255, 0.05) 0%, transparent 70%);
        }
        
        .world-svg path {
            fill: rgba(45, 36, 56, 0.6);
            stroke: rgba(0, 212, 255, 0.3);
            stroke-width: 0.5;
            transition: fill 0.3s;
        }
        
        .world-svg path:hover {
            fill: rgba(0, 212, 255, 0.2);
        }
        
        .world-svg path.threat-country {
            fill: rgba(239, 68, 68, 0.3);
            stroke: var(--threat-critical);
            stroke-width: 1;
        }
        
        .threat-indicators {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }
        
        .threat-marker {
            position: absolute;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            border: 2px solid;
            animation: threatPulse 2s infinite;
            cursor: pointer;
            pointer-events: all;
        }
        
        .threat-marker.critical {
            background: var(--threat-critical);
            border-color: var(--threat-critical);
            box-shadow: 0 0 20px rgba(239, 68, 68, 0.6);
        }
        
        .threat-marker.high {
            background: var(--threat-warning);
            border-color: var(--threat-warning);
            box-shadow: 0 0 15px rgba(245, 158, 11, 0.6);
        }
        
        .threat-marker.medium {
            background: var(--quantum-cyan);
            border-color: var(--quantum-cyan);
            box-shadow: 0 0 10px rgba(6, 182, 212, 0.6);
        }
        
        .threat-marker.blocked {
            background: var(--threat-safe);
            border-color: var(--threat-safe);
            box-shadow: 0 0 8px rgba(16, 185, 129, 0.6);
        }
        
        @keyframes threatPulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.3); opacity: 0.7; }
        }
        
        .threat-tooltip {
            position: absolute;
            background: rgba(12, 10, 26, 0.95);
            border: 1px solid var(--quantum-blue);
            border-radius: 6px;
            padding: 0.5rem;
            font-size: 0.8rem;
            color: #e5e7eb;
            pointer-events: none;
            z-index: 1000;
            transform: translate(-50%, -100%);
            margin-top: -10px;
            min-width: 200px;
        }
        
        .map-legend {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 1rem;
            flex-wrap: wrap;
        }
        
        .legend-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
        }
        
        .legend-color {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            border: 2px solid;
        }
        
        .legend-color.critical {
            background: var(--threat-critical);
            border-color: var(--threat-critical);
        }
        
        .legend-color.high {
            background: var(--threat-warning);
            border-color: var(--threat-warning);
        }
        
        .legend-color.medium {
            background: var(--quantum-cyan);
            border-color: var(--quantum-cyan);
        }
        
        .legend-color.blocked {
            background: var(--threat-safe);
            border-color: var(--threat-safe);
        }
        
        .threat-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }
        
        .stat-item {
            text-align: center;
            background: rgba(45, 36, 56, 0.4);
            padding: 1rem;
            border-radius: 8px;
            border: 1px solid rgba(0, 212, 255, 0.2);
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--quantum-blue);
            margin-bottom: 0.5rem;
        }
        
        .stat-label {
            color: #9ca3af;
            font-size: 0.9rem;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .nav-links { display: none; }
            .metrics-grid { grid-template-columns: 1fr; }
            .pricing-grid { grid-template-columns: 1fr; }
            
            /* Mobile threat map */
            #world-map { height: 300px; }
            .map-legend { 
                gap: 1rem;
                justify-content: space-around;
            }
            .legend-item { 
                font-size: 0.8rem;
                gap: 0.3rem;
            }
            .threat-stats { 
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            .threat-tooltip {
                font-size: 0.75rem;
                min-width: 150px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="nav">
        <div class="container">
            <div class="nav-content">
                <div class="logo">⚡ Quantum Aegis</div>
                <ul class="nav-links">
                    <li><a href="#dashboard">Dashboard</a></li>
                    <li><a href="#threat-map">Threat Map</a></li>
                    <li><a href="#threats">Threats</a></li>
                    <li><a href="#pricing">Pricing</a></li>
                </ul>
                <a href="#pricing" class="cta-button">Get Started</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1>Enterprise Cyber Defense Platform</h1>
            <p>Advanced AI-powered threat detection with quantum encryption and real-time security monitoring for enterprise-grade protection.</p>
            <a href="#pricing" class="cta-button">Start Free Trial</a>
        </div>
    </section>

    <!-- Dashboard -->
    <section id="dashboard" class="dashboard">
        <div class="container">
            <h2 class="section-title">Real-Time Security Metrics</h2>
            <div class="metrics-grid">
                <div class="metric-card">
                    <div class="metric-label">Threats Blocked</div>
                    <div class="metric-value" id="threats-blocked">2,847</div>
                    <div class="metric-change">↑ 12% today</div>
                </div>
                <div class="metric-card">
                    <div class="metric-label">Mean Time to Detect</div>
                    <div class="metric-value" id="mttd">1.2s</div>
                    <div class="metric-change">↓ 0.3s improved</div>
                </div>
                <div class="metric-card">
                    <div class="metric-label">AI Confidence</div>
                    <div class="metric-value" id="ai-confidence">97.8%</div>
                    <div class="metric-change">↑ 2.1% accuracy</div>
                </div>
                <div class="metric-card">
                    <div class="metric-label">Quantum Keys</div>
                    <div class="metric-value" id="quantum-keys">48</div>
                    <div class="metric-change pulse">🔄 Rotating</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Global Threat Map -->
    <section id="threat-map" class="dashboard">
        <div class="container">
            <div class="map-section">
                <h2 class="section-title">🌍 Global Threat Map - Live Cyber Attacks</h2>
                <div class="map-container">
                    <div id="world-map">
                        <svg viewBox="0 0 1000 500" class="world-svg">
                            <!-- World Map SVG will be populated by JavaScript -->
                        </svg>
                        <div class="threat-indicators" id="threat-indicators">
                            <!-- Threat markers will be added here -->
                        </div>
                    </div>
                    <div class="map-legend">
                        <div class="legend-item">
                            <div class="legend-color critical"></div>
                            <span>Critical Threats</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color high"></div>
                            <span>High Risk</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color medium"></div>
                            <span>Medium Risk</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color blocked"></div>
                            <span>Blocked</span>
                        </div>
                    </div>
                </div>
                <div class="threat-stats">
                    <div class="stat-item">
                        <div class="stat-value" id="countries-affected">47</div>
                        <div class="stat-label">Countries Affected</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="active-campaigns">12</div>
                        <div class="stat-label">Active Campaigns</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="threats-per-minute">23</div>
                        <div class="stat-label">Threats/Minute</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Threats -->
    <section id="threats" class="dashboard">
        <div class="container">
            <div class="threats-section">
                <h2 class="section-title">Active Threat Intelligence</h2>
                <div id="threats-list">
                    <!-- Threats will be populated by JavaScript -->
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing -->
    <section id="pricing" class="pricing">
        <div class="container">
            <h2>Choose Your Protection Level</h2>
            <div class="pricing-grid">
                <div class="pricing-card">
                    <div class="plan-name">Lite</div>
                    <div class="plan-price">$49<span style="font-size: 1rem; color: #9ca3af;">/month</span></div>
                    <ul class="plan-features">
                        <li>✓ Basic threat detection</li>
                        <li>✓ Email protection</li>
                        <li>✓ 24/7 monitoring</li>
                        <li>✓ Monthly reports</li>
                    </ul>
                    <button class="select-plan" onclick="selectPlan('lite')">Select Lite</button>
                </div>
                <div class="pricing-card">
                    <div class="plan-name">Pro</div>
                    <div class="plan-price">$149<span style="font-size: 1rem; color: #9ca3af;">/month</span></div>
                    <ul class="plan-features">
                        <li>✓ Advanced AI detection</li>
                        <li>✓ Quantum encryption</li>
                        <li>✓ Custom policies</li>
                        <li>✓ API access</li>
                        <li>✓ Priority support</li>
                    </ul>
                    <button class="select-plan" onclick="selectPlan('pro')">Select Pro</button>
                </div>
                <div class="pricing-card">
                    <div class="plan-name">Enterprise</div>
                    <div class="plan-price">$399<span style="font-size: 1rem; color: #9ca3af;">/month</span></div>
                    <ul class="plan-features">
                        <li>✓ Full platform access</li>
                        <li>✓ Dedicated support</li>
                        <li>✓ Custom integration</li>
                        <li>✓ Compliance reporting</li>
                        <li>✓ White-label options</li>
                    </ul>
                    <button class="select-plan" onclick="selectPlan('enterprise')">Select Enterprise</button>
                </div>
            </div>
        </div>
    </section>

    <script>
        // API Base URL
        const API_BASE = '/api';
        
        // Global threat locations with realistic cyber threat hotspots
        const THREAT_LOCATIONS = {
            'Russia': { x: 600, y: 140, country: 'Russia', threats: ['APT', 'Ransomware', 'State-sponsored'] },
            'China': { x: 700, y: 200, country: 'China', threats: ['APT', 'Espionage', 'Malware'] },
            'North Korea': { x: 720, y: 190, country: 'North Korea', threats: ['Ransomware', 'Cryptomining'] },
            'Iran': { x: 580, y: 220, country: 'Iran', threats: ['APT', 'Infrastructure attacks'] },
            'USA': { x: 200, y: 180, country: 'USA', threats: ['Cybercrime', 'Financial fraud'] },
            'Brazil': { x: 300, y: 320, country: 'Brazil', threats: ['Banking trojans', 'Fraud'] },
            'Nigeria': { x: 500, y: 280, country: 'Nigeria', threats: ['Financial fraud', 'Scams'] },
            'Romania': { x: 550, y: 160, country: 'Romania', threats: ['Cybercrime', 'Malware'] },
            'Ukraine': { x: 570, y: 150, country: 'Ukraine', threats: ['Cyberwarfare', 'APT'] },
            'India': { x: 650, y: 240, country: 'India', threats: ['Mobile malware', 'Fraud'] },
            'Germany': { x: 520, y: 140, country: 'Germany', threats: ['Industrial espionage'] },
            'UK': { x: 480, y: 130, country: 'UK', threats: ['Financial threats'] },
            'South Korea': { x: 730, y: 200, country: 'South Korea', threats: ['APT', 'Spyware'] },
            'Turkey': { x: 560, y: 200, country: 'Turkey', threats: ['Political malware'] },
            'Vietnam': { x: 680, y: 240, country: 'Vietnam', threats: ['APT', 'Espionage'] }
        };
        
        // Simplified world map SVG paths (key countries/regions)
        const WORLD_MAP_SVG = `
            <!-- Russia -->
            <path d="M580,120 L780,120 L780,180 L680,200 L580,160 Z" class="country" data-country="Russia"/>
            <!-- China -->
            <path d="M680,180 L780,180 L780,240 L680,240 Z" class="country" data-country="China"/>
            <!-- USA -->
            <path d="M120,160 L280,160 L280,220 L120,220 Z" class="country" data-country="USA"/>
            <!-- Europe -->
            <path d="M480,120 L580,120 L580,200 L480,200 Z" class="country" data-country="Europe"/>
            <!-- Brazil -->
            <path d="M280,300 L360,300 L360,380 L280,380 Z" class="country" data-country="Brazil"/>
            <!-- Africa -->
            <path d="M480,240 L580,240 L580,350 L480,350 Z" class="country" data-country="Africa"/>
            <!-- India -->
            <path d="M620,220 L680,220 L680,280 L620,280 Z" class="country" data-country="India"/>
            <!-- Australia -->
            <path d="M720,320 L800,320 L800,380 L720,380 Z" class="country" data-country="Australia"/>
            <!-- Japan -->
            <path d="M780,180 L820,180 L820,220 L780,220 Z" class="country" data-country="Japan"/>
        `;
        
        let activeThreatMarkers = [];
        let threatTooltip = null;
        
        // Initialize world map
        function initializeWorldMap() {
            const worldSvg = document.querySelector('.world-svg');
            worldSvg.innerHTML = WORLD_MAP_SVG;
            
            // Add click handlers to countries
            document.querySelectorAll('.country').forEach(country => {
                country.addEventListener('click', (e) => {
                    const countryName = e.target.getAttribute('data-country');
                    showCountryThreatInfo(countryName);
                });
            });
        }
        
        // Update global threat map with real VirusTotal data
        async function updateThreatMap() {
            try {
                const response = await fetch(`${API_BASE}/demo/threats`);
                const threats = await response.json();
                
                // Clear existing markers
                const indicatorsContainer = document.getElementById('threat-indicators');
                indicatorsContainer.innerHTML = '';
                activeThreatMarkers = [];
                
                // Add threat markers based on realistic distribution
                const countries = Object.keys(THREAT_LOCATIONS);
                const usedLocations = new Set();
                
                threats.forEach((threat, index) => {
                    // Assign threats to realistic countries based on threat type
                    let selectedCountry;
                    
                    if (threat.type === 'ransomware') {
                        selectedCountry = ['Russia', 'North Korea', 'Iran'][Math.floor(Math.random() * 3)];
                    } else if (threat.type === 'apt') {
                        selectedCountry = ['China', 'Russia', 'Iran', 'North Korea'][Math.floor(Math.random() * 4)];
                    } else if (threat.type === 'trojan' || threat.type === 'malware') {
                        selectedCountry = ['Russia', 'Ukraine', 'Romania', 'Brazil'][Math.floor(Math.random() * 4)];
                    } else if (threat.type === 'cryptominer') {
                        selectedCountry = ['Russia', 'China', 'North Korea'][Math.floor(Math.random() * 3)];
                    } else {
                        // Random selection for other types
                        selectedCountry = countries[Math.floor(Math.random() * countries.length)];
                    }
                    
                    // Avoid duplicate locations by adding slight offset
                    let location = THREAT_LOCATIONS[selectedCountry];
                    let xOffset = usedLocations.has(selectedCountry) ? (Math.random() - 0.5) * 40 : 0;
                    let yOffset = usedLocations.has(selectedCountry) ? (Math.random() - 0.5) * 30 : 0;
                    
                    usedLocations.add(selectedCountry);
                    
                    const marker = createThreatMarker(
                        location.x + xOffset,
                        location.y + yOffset,
                        threat,
                        selectedCountry
                    );
                    
                    indicatorsContainer.appendChild(marker);
                    activeThreatMarkers.push({
                        element: marker,
                        threat: threat,
                        country: selectedCountry,
                        x: location.x + xOffset,
                        y: location.y + yOffset
                    });
                    
                    // Animate marker appearance
                    setTimeout(() => {
                        marker.style.opacity = '1';
                        marker.style.transform = 'scale(1)';
                    }, index * 200);
                });
                
                // Update statistics
                updateMapStatistics(threats);
                
            } catch (error) {
                console.log('Error updating threat map:', error);
            }
        }
        
        // Create threat marker element
        function createThreatMarker(x, y, threat, country) {
            const marker = document.createElement('div');
            marker.className = `threat-marker ${threat.threat_level || 'medium'}`;
            marker.style.left = `${x}px`;
            marker.style.top = `${y}px`;
            marker.style.opacity = '0';
            marker.style.transform = 'scale(0)';
            marker.style.transition = 'all 0.5s ease';
            
            // Add tooltip functionality
            marker.addEventListener('mouseenter', (e) => {
                showThreatTooltip(e, threat, country);
            });
            
            marker.addEventListener('mouseleave', () => {
                hideThreatTooltip();
            });
            
            return marker;
        }
        
        // Show threat tooltip
        function showThreatTooltip(event, threat, country) {
            hideThreatTooltip(); // Remove existing tooltip
            
            threatTooltip = document.createElement('div');
            threatTooltip.className = 'threat-tooltip';
            threatTooltip.innerHTML = `
                <div style="font-weight: 600; color: var(--quantum-blue); margin-bottom: 0.5rem;">
                    ${threat.title}
                </div>
                <div style="margin-bottom: 0.3rem;">
                    <span style="color: var(--threat-critical);">Origin:</span> ${country}
                </div>
                <div style="margin-bottom: 0.3rem;">
                    <span style="color: var(--threat-warning);">Type:</span> ${threat.type}
                </div>
                <div style="margin-bottom: 0.3rem;">
                    <span style="color: var(--quantum-cyan);">Status:</span> ${threat.status}
                </div>
                ${threat.engines_detected ? `<div style="margin-bottom: 0.3rem;">
                    <span style="color: var(--threat-safe);">Engines:</span> ${threat.engines_detected} detected
                </div>` : ''}
                <div style="font-size: 0.75rem; color: #9ca3af;">
                    ${new Date(threat.timestamp).toLocaleString()}
                </div>
            `;
            
            const rect = event.target.getBoundingClientRect();
            const mapRect = document.getElementById('world-map').getBoundingClientRect();
            
            threatTooltip.style.left = `${rect.left - mapRect.left}px`;
            threatTooltip.style.top = `${rect.top - mapRect.top}px`;
            
            document.getElementById('world-map').appendChild(threatTooltip);
        }
        
        // Hide threat tooltip
        function hideThreatTooltip() {
            if (threatTooltip) {
                threatTooltip.remove();
                threatTooltip = null;
            }
        }
        
        // Update map statistics
        function updateMapStatistics(threats) {
            const countriesAffected = new Set(activeThreatMarkers.map(m => m.country)).size;
            const activeCampaigns = threats.filter(t => t.status === 'INVESTIGATING').length + 
                                   Math.floor(Math.random() * 8) + 5;
            const threatsPerMinute = Math.floor(15 + Math.random() * 15);
            
            document.getElementById('countries-affected').textContent = countriesAffected;
            document.getElementById('active-campaigns').textContent = activeCampaigns;
            document.getElementById('threats-per-minute').textContent = threatsPerMinute;
        }
        
        // Show country threat information
        function showCountryThreatInfo(countryName) {
            const countryThreats = activeThreatMarkers.filter(m => m.country === countryName);
            if (countryThreats.length > 0) {
                const threatTypes = countryThreats.map(t => t.threat.type);
                const uniqueTypes = [...new Set(threatTypes)];
                alert(`${countryName} - Active Threats: ${uniqueTypes.join(', ')}\nTotal: ${countryThreats.length} threats detected`);
            }
        }
        
        // Update metrics every 5 seconds
        async function updateMetrics() {
            try {
                const response = await fetch(`${API_BASE}/demo/metrics`);
                const data = await response.json();
                
                document.getElementById('threats-blocked').textContent = data.threatsBlocked?.toLocaleString() || '2,847';
                document.getElementById('mttd').textContent = `${data.mttd || '1.2'}s`;
                document.getElementById('ai-confidence').textContent = `${data.aiConfidence || '97.8'}%`;
                document.getElementById('quantum-keys').textContent = data.quantumKeys || '48';
            } catch (error) {
                console.log('Demo mode - using static data');
            }
        }
        
        // Update threats list
        async function updateThreats() {
            try {
                const response = await fetch(`${API_BASE}/demo/threats`);
                const threats = await response.json();
                
                const threatsList = document.getElementById('threats-list');
                threatsList.innerHTML = threats.map(threat => `
                    <div class="threat-item">
                        <div class="threat-title">${threat.title}</div>
                        <div class="threat-meta">
                            <span>Source: ${threat.source}</span>
                            <span>Type: ${threat.type}</span>
                            <span class="status-badge">${threat.status}</span>
                            ${threat.engines_detected ? `<span style="color: var(--quantum-cyan);">${threat.engines_detected} engines</span>` : ''}
                        </div>
                    </div>
                `).join('');
            } catch (error) {
                document.getElementById('threats-list').innerHTML = `
                    <div class="threat-item">
                        <div class="threat-title">Advanced Malware Detection</div>
                        <div class="threat-meta">
                            <span>Source: Email Gateway</span>
                            <span>Type: malware</span>
                            <span class="status-badge">BLOCKED</span>
                        </div>
                    </div>
                `;
            }
        }
        
        // Plan selection
        function selectPlan(plan) {
            alert(`Redirecting to ${plan} subscription checkout...`);
            // In production, this would redirect to Stripe checkout
        }
        
        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            initializeWorldMap();
            updateMetrics();
            updateThreats();
            updateThreatMap();
            
            // Update every 5-10 seconds
            setInterval(updateMetrics, 5000);
            setInterval(updateThreats, 10000);
            setInterval(updateThreatMap, 15000); // Update map every 15 seconds
        });
        
        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
