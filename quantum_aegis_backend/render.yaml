services:

type: web
name: quantum-aegis-backend
env: python
startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
buildCommand: pip install -r requirements.txt
envVars:

key: PORT
value: 10000
